#!/usr/bin/python3


def best_score(a_dictionary):

    if (a_dictionary):
        max = 0
        max = max(a_dictionary.keys())

        if max:
            return max
        else:
            return None
    return None
